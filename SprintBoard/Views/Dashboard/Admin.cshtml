@model SprintBoard.Models.AdminDashboardViewModel
@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="welcome-banner mb-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
        <div>
            <h4>Welcome back, @Model.FullName!</h4>
            <p>Manage your team and track user activity.</p>
        </div>
        <a href="@Url.Action("Create", "Users")" class="btn btn-light btn-sm fw-semibold">
            <i class="fas fa-user-plus me-1"></i> New User
        </a>
    </div>
</div>

<!-- Stats -->
<div class="row g-3 mb-4">
    <div class="col-6 col-lg-4">
        <div class="stat-card stat-blue">
            <div class="s-value">@Model.TotalUsersCreated</div>
            <div class="s-label">Users Created</div>
            <i class="fas fa-users s-icon"></i>
        </div>
    </div>
    <div class="col-6 col-lg-4">
        <div class="stat-card stat-green">
            <div class="s-value">@Model.ActiveUsers</div>
            <div class="s-label">Active Users</div>
            <i class="fas fa-user-check s-icon"></i>
        </div>
    </div>
    <div class="col-6 col-lg-4">
        <div class="stat-card stat-purple">
            <div class="s-value">@Model.ThisMonthUsers</div>
            <div class="s-label">Joined This Month</div>
            <i class="fas fa-calendar-plus s-icon"></i>
        </div>
    </div>
</div>

<!-- My Users + Quick Actions -->
<div class="row g-3">
    <div class="col-lg-8">
        <div class="dash-card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <span><i class="fas fa-users me-2 text-muted"></i>My Users</span>
                <a href="@Url.Action("Index", "Users")" class="btn btn-sm btn-outline-primary">View All</a>
            </div>
            <div class="table-responsive">
                <table class="table dash-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Status</th>
                            <th>Joined</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.RecentUsers.Any())
                        {
                            @foreach (var u in Model.RecentUsers)
                            {
                                <tr>
                                    <td class="fw-semibold">@u.FullName</td>
                                    <td class="text-muted">@u.Email</td>
                                    <td><span class="badge bg-success">Active</span></td>
                                    <td class="text-muted">@u.CreatedAt.ToString("MMM dd, yyyy")</td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="4" class="text-center text-muted py-4">
                                    No users yet.
                                    <a href="@Url.Action("Create", "Users")">Create your first user.</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="dash-card h-100">
            <div class="card-header"><i class="fas fa-bolt me-2 text-muted"></i>Quick Actions</div>
            <div class="p-3">
                <a href="@Url.Action("Create", "Users")" class="qa-btn">
                    <i class="fas fa-user-plus"></i> Create User
                </a>
                <a href="@Url.Action("Index", "Users")" class="qa-btn">
                    <i class="fas fa-users"></i> Manage Users
                </a>
                <a asp-area="Identity" asp-page="/Account/Manage/Index" class="qa-btn">
                    <i class="fas fa-user-circle"></i> My Profile
                </a>
                <a asp-area="Identity" asp-page="/Account/Manage/ChangePassword" class="qa-btn">
                    <i class="fas fa-key"></i> Change Password
                </a>
            </div>
        </div>
    </div>
</div>
